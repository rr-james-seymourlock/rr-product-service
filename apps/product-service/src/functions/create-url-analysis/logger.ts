/**
 * Logger instance for create-url-analysis endpoint
 */
import { createLogger } from '@rr/shared/utils';

export { createLogger };

export const logger = createLogger('product-service.create-url-analysis');
