/**
 * Logger instance for extract-product-ids endpoint
 */
import { createLogger } from '@rr/shared/utils';

export { createLogger };

export const logger = createLogger('product-service.extract-product-ids');
