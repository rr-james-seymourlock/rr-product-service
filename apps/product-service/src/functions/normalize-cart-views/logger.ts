/**
 * Logger instance for normalize-cart-views endpoint
 */
import { createLogger } from '@rr/shared/utils';

export { createLogger };

export const logger = createLogger('product-service.normalize-cart-views');
