/**
 * Logger instance for normalize-product-views endpoint
 */
import { createLogger } from '@rr/shared/utils';

export { createLogger };

export const logger = createLogger('product-service.normalize-product-views');
