import { createLogger } from '@rr/shared/utils';

/**
 * Logger instance for the ASIN converter package
 *
 * Namespace: 'asin-converter'
 *
 * All logs from this package will include the namespace field
 * for easy filtering and debugging.
 */
export const logger = createLogger('asin-converter');
